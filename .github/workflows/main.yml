name: Robot Snake 3DS
on: [push]

jobs:
  activation:
    runs-on: ubuntu-latest
    steps:
      - name: Verifier le depot
        uses: actions/checkout@v3

      - name: Message de confirmation
        run: echo "Le robot est pret !"
